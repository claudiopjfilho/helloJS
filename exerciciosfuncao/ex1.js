/*01) Crie uma função que dado dois valores (passados como parâmetros) mostre no console a soma, subtração,
multiplicação e divisão desses valores.*/

function problemas(x, y) {
         return {
        soma: x + y,
        subtracao: x - y,
        mult: x * y,
        div: x / y

        }
    }

console.log(problemas(3, 2))
    


